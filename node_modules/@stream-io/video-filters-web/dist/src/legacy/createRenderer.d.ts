import { TFLite } from './tflite';
import { SegmentationLevel } from './segmentation';
import { BackgroundBlurLevel, BackgroundFilter } from '../types';
export type Renderer = {
    /**
     * Disposes of the renderer.
     */
    dispose: () => void;
};
export declare function createRenderer(tflite: TFLite, videoSource: HTMLVideoElement, targetCanvas: HTMLCanvasElement, options: {
    backgroundFilter: BackgroundFilter;
    segmentationLevel?: SegmentationLevel;
    backgroundImage?: HTMLImageElement;
    backgroundBlurLevel?: BackgroundBlurLevel;
    fps?: number;
}, onError?: (error: any) => void): Renderer;
