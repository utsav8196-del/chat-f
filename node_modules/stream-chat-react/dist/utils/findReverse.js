// last own message should be tracked in the low-level client
export const findReverse = (items, matches) => {
    for (let i = items.length - 1; i >= 0; i -= 1) {
        if (matches(items[i])) {
            return items[i];
        }
    }
    return undefined;
};
